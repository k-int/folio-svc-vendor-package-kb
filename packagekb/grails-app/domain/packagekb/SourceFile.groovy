package packagekb


/**
 *  A TEMPORARY area where package files are uploaded and held whilst they are being processed.
 *  Packages are loaded here as they work through the asynchronous upload process.
 *
 */
class SourceFile {

    static constraints = {
    }
}
